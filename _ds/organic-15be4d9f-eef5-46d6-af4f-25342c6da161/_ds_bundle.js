/* @ds-bundle: {"format":4,"namespace":"Organic_organi","components":[],"sourceHashes":{},"inlinedExternals":[],"unexposedExports":[]} */
/* Organic design system — CSS-based component library */
/* All components are defined in styles.css as classes: .btn, .card, .field, .nav, .table, .dialog, .tag, .seg */

(() => {
  const __ds_ns = (window.Organic_organi = window.Organic_organi || {});
  const __ds_scope = {};
  (__ds_ns.__errors = __ds_ns.__errors || []);

  /* Design system metadata */
  __ds_ns.components = {
    btn: "Primary action button — use class .btn with .btn-primary, .btn-secondary, .btn-ghost",
    card: "Surface-filled content card — use class .card with .card-kicker, .card-title, .card-body, .card-meta",
    field: "Form field wrapper — use class .field with native input/select/textarea",
    nav: "Header navigation bar — use class .nav with .nav-brand",
    table: "Data table — use class .table",
    dialog: "Modal dialog — use class .dialog-backdrop with .dialog, .dialog-title, .dialog-body, .dialog-actions",
    tag: "Small label — use class .tag with .tag-accent, .tag-accent-2, .tag-neutral, .tag-outline"
  };

  __ds_ns.version = "1.0.0";
  __ds_ns.ready = true;
})();
